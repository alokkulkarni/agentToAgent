# Core dependencies
pydantic==2.10.5
pydantic-settings==2.7.1

# AWS Bedrock
boto3==1.35.94
botocore==1.35.94

# Async support
aioboto3==13.3.0

# Configuration
python-dotenv==1.0.1

# Identity Provider & Authentication
PyJWT[crypto]>=2.8.0
cryptography>=41.0.0
httpx>=0.24.1

# Logging
structlog==24.4.0
colorama==0.4.6

# Utilities
typing-extensions==4.12.2

# ── HA / Multi-instance distributed state ────────────────────────────────────
# Redis client for shared workflow state, ownership, and WebSocket pub/sub.
# Required when HA_BACKEND=redis (default: in_memory, no extra deps).
redis>=5.0.0

# ── Shared workflow DB backend — PostgreSQL ───────────────────────────────────
# Required when WORKFLOW_DB_BACKEND=postgresql (SQLite is default).
psycopg2-binary>=2.9.9

# Development
mypy==1.14.0
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
black==26.3.1
ruff==0.8.4
